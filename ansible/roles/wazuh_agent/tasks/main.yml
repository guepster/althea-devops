---
# Rôle wazuh_agent — déployé via Ansible (cf. DAT).
# TODO: compléter les tâches (extrait non détaillé dans le DAT).
- name: "wazuh_agent | placeholder"
  ansible.builtin.debug: { msg: "role wazuh_agent" }
